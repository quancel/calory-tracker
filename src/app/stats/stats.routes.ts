import { Routes } from '@angular/router';
import { StatsShellComponent } from './components/stats-shell/stats-shell.component';

export const STATS_ROUTES: Routes = [{ path: '', component: StatsShellComponent }];
